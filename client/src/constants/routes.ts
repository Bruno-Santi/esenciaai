enum ROUTES {
  landing = "/",
  onboarding = "/onboarding",
  dashboard = "/dashboard",
  login = "/auth/login",
  register = "/auth/register",
  auth = "/auth/",
  members = "/members",
  survey = "/survey",
  retro = "/retro",
}

export default ROUTES;
